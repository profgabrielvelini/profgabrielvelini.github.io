import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const selections=sqliteTable("selections",{cardId:integer("card_id").primaryKey(),teamName:text("team_name").notNull(),deviceId:text("device_id").notNull(),round:integer("round").notNull().default(1),selectedAt:text("selected_at").notNull()},table=>[uniqueIndex("selections_device_round_unique").on(table.deviceId,table.round)]);
export const gameState=sqliteTable("game_state",{id:integer("id").primaryKey(),currentRound:integer("current_round").notNull().default(1),timerStatus:text("timer_status").notNull().default("idle"),timerDuration:integer("timer_duration").notNull().default(300),timerRemaining:integer("timer_remaining").notNull().default(300),timerEndAt:integer("timer_end_at"),pinHash:text("pin_hash")});
